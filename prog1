#include <stdio.h>

int main() {
    
    float temp_c;
    float temp_f;
    printf("Введите темпеатуру в градусах цельсия:");
    scanf("%f", &temp_c);
    temp_f = (temp_c * 9/5) + 32;
    printf("C = %.2f ; F = %.2f\n", temp_c, temp_f);

    return 0;
}
