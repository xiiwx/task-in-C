#include <stdio.h>

int main() {
    
    int a, b, c;
    
    printf("Введите три числа: ");
    scanf("%d %d %d", &a, &b, &c);
    
    int m = a * b * c;
    int sign = (m > 0) - (m < 0);
    
    printf("%d\n", sign);
    
    
return 0;
    
}
